# Required metadata
sonar.projectKey=python-mini-project   # A unique identifier for your project in SonarQube.
sonar.projectName=Python-test  # The display name of the project in the SonarQube dashboard.
sonar.projectVersion=1.0       # Specifies the current version of your project.

# Project source encoding
sonar.sourceEncoding=UTF-8     # The character encoding of the source files, typically UTF-8.

# Main source directory
sonar.sources=.                # The directory containing your source code. '.' refers to the current directory.

# Python version
sonar.python.version=3.x       # Specifies the version of Python used in your project (e.g., 3.8).

# Optional: Specify the language explicitly (SonarQube can usually detect this automatically)
sonar.language=py              # The programming language of the project, explicitly set to Python.

# Exclude specific files or directories (e.g., virtual environment)
sonar.exclusions=**/__pycache__/**, **/*.pyc, **/venv/**   
# Excludes specific files or directories from analysis: '__pycache__', compiled Python files ('*.pyc'), and virtual environments ('venv').

# Coverage and test reports
# Uncomment and set the correct paths for your coverage and test reports.
# sonar.python.coverage.reportPaths=coverage/coverage.xml
# Specifies the path to the coverage report generated by tools like coverage.py.
# sonar.python.xunit.reportPath=reports/test-results.xml
# Specifies the path to xUnit-style test reports, typically generated by testing frameworks like pytest.

# SonarQube server URL (optional if provided via CLI or environment variables)
sonar.host.url=http://4.240.124.40:9000/  
# The URL of your SonarQube server.

# Authentication token (optional if provided via CLI or environment variables)
sonar.login=sqa_18ca7a4a75620b175378099dc7730a1eddf2a3b6   
# The authentication token used to connect to SonarQube.

# Optional: Additional properties for advanced configurations
sonar.verbose=true   # Enables verbose logging during the analysis process for more detailed output.
